sudo: false
language: cpp
cache:
  - apt
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-4.9
      - g++-4.9

script:
  - rm -rf build;
  - CXX=g++-4.9 ./waf configure;
  - ./waf build;
